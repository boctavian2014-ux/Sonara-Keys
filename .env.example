# Copiază acest fișier ca `.env` și înlocuiește placeholder-ele.
# După modificări: npx expo start --clear

# Server Python Basic Pitch (POST /transcribe) — URL fără slash la final
# - Telefon fizic (aceeași Wi‑Fi ca PC-ul): http://192.168.x.x:8787
# - Emulator Android pe același PC: http://10.0.2.2:8787
EXPO_PUBLIC_TRANSCRIBE_API_URL=http://YOUR_PC_LAN_IP:8787

# Portativ mare (SVG): același host ca server-ts (Railway) cu POST /render-score-svg + LilyPond în Docker.
# Dacă lipsește, se folosește EXPO_PUBLIC_TRANSCRIBE_API_URL — acolo TREBUIE să existe /render-score-svg sau vei vedea 404.
# EXPO_PUBLIC_SCORE_RENDER_API_URL=https://your-service.up.railway.app

# Opțional: pune 1 ca să sari peste Basic Pitch pe telefon (TensorFlow)
# EXPO_PUBLIC_SKIP_ONDEVICE_BASIC_PITCH=1

# GPU RunPod / server-gpu — streaming (același secret ca PIANO_GPU_API_KEY pe server, dacă îl activezi acolo)
# EXPO_PUBLIC_PIANO_GPU_API_URL=https://xxxxx-8789.proxy.runpod.net
# EXPO_PUBLIC_PIANO_GPU_STREAMING=1
# EXPO_PUBLIC_PIANO_GPU_API_KEY=your-long-random-string
# Fereastră / pas mai mici = mai puțin audio per request (default ~0.72s / 0.36s) — util dacă primești 502
# EXPO_PUBLIC_PIANO_GPU_WINDOW_SEC=0.55
# EXPO_PUBLIC_PIANO_GPU_HOP_SEC=0.28
# Timeout client pentru un singur POST /transcribe-window (ms), default 120000
# EXPO_PUBLIC_PIANO_GPU_FETCH_TIMEOUT_MS=180000

# Kinde (conturi) — https://kinde.com — din aplicația Kinde (Frontend app / Native)
# EXPO_PUBLIC_KINDE_DOMAIN=https://YOUR_SUBDOMAIN.kinde.com
# EXPO_PUBLIC_KINDE_CLIENT_ID=your_native_client_id
# Opțional: pune 1 ca să blochezi app-ul la ecranul de login până te autentifici (implicit poți folosi app-ul fără cont)
# EXPO_PUBLIC_KINDE_REQUIRE_LOGIN=1
# În Kinde → Applications → Callback URLs adaugă: sonarakeys://kinde_callback
# Opțional (social direct pe butoane dedicate): connection IDs din Kinde Authentication
# EXPO_PUBLIC_KINDE_CONNECTION_ID_GOOGLE=conn_xxx
# EXPO_PUBLIC_KINDE_CONNECTION_ID_APPLE=conn_xxx
# EXPO_PUBLIC_KINDE_CONNECTION_ID_FACEBOOK=conn_xxx

# Supabase — https://supabase.com/dashboard/project/hctlyggquhrxmdeiolmc
# Settings → API: Project URL + anon public key
# EXPO_PUBLIC_SUPABASE_URL=https://hctlyggquhrxmdeiolmc.supabase.co
# EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
